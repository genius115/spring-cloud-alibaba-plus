<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiao.mai.dao.TestMapper">
    <select id="test" resultType="java.lang.String">
        select COUNT(*) from dual
    </select>
    <select id="seluser" parameterType="java.util.HashMap" resultType="java.util.HashMap">
    	select * from tb_user
    </select>
    <select id="seluserpage" parameterType="java.util.HashMap" resultType="com.xiao.mai.bean.TbUserBean">
    	select user_id AS userId,user_name AS userName,age AS age,card_id AS cardId  
    	from tb_user
    </select>
    <insert id="insuser">
    	insert into tb_user (user_id,user_name) value (#{id},#{name})
    </insert>
    <update id="upduser">
    	update tb_user set user_name=#{name} where user_id=#{id}
    </update>
    <delete id="deluser">
    	delete from tb_user where user_id=#{id}
    </delete>
</mapper>